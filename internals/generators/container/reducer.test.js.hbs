/*
 *
 * Test {{ properCase name }} Reducer
 *
 */

import {{ camelCase name }}Reducer from '../reducer';

describe('{{ camelCase name }}Reducer', () => {
  it('should return the initial state', () => {
    expect({{ camelCase name }}Reducer(undefined, {})).toEqual({});
  });
});
